---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.11.3
  creationTimestamp: null
  name: bindings.control.kubestellar.io
spec:
  group: control.kubestellar.io
  names:
    kind: Binding
    listKind: BindingList
    plural: bindings
    shortNames:
    - bdg
    singular: binding
  scope: Cluster
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Binding is mapped 1:1 to a single BindingPolicy object. Binding
          reflects the resolution of the BindingPolicy's selectors, and explicitly
          reflects which objects should go to what destinations.
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: '`spec` explicitly describes a desired binding between workloads
              and Locations. It reflects the resolution of a BindingPolicy''s selectors.'
            properties:
              destinations:
                description: '`destinations` is a list of cluster-identifiers that
                  the objects should be propagated to.'
                items:
                  description: Destination wraps the identifiers required to uniquely
                    identify a destination cluster.
                  properties:
                    clusterId:
                      type: string
                  type: object
                type: array
              workload:
                description: '`Workload` is a collection of namespaced-scoped objects
                  and a collection of cluster-scoped objects to be propagated to destination
                  clusters.'
                properties:
                  clusterScope:
                    description: '`clusterScope` holds a list of individual cluster-scoped
                      objects to downsync.'
                    items:
                      description: ClusterScopeDownsyncObject matches one particular
                        cluster scoped object.
                      properties:
                        group:
                          type: string
                        name:
                          type: string
                        resource:
                          type: string
                        resourceVersion:
                          type: string
                        version:
                          type: string
                      required:
                      - group
                      - name
                      - resource
                      - resourceVersion
                      - version
                      type: object
                    type: array
                  namespaceScope:
                    description: '`NamespaceScope` holds a list of individual namespace-scoped
                      objects to downsync.'
                    items:
                      description: NamespaceScopeDownsyncObject matches one particular
                        namespace scoped object.
                      properties:
                        group:
                          type: string
                        name:
                          type: string
                        namespace:
                          type: string
                        resource:
                          type: string
                        resourceVersion:
                          type: string
                        version:
                          type: string
                      required:
                      - group
                      - name
                      - namespace
                      - resource
                      - resourceVersion
                      - version
                      type: object
                    type: array
                type: object
            type: object
        type: object
    served: true
    storage: true
