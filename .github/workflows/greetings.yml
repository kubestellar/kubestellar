name: Greetings

on:
  pull_request_target:
    types: [opened, reopened]
  issues:
    types: [opened, reopened]

permissions:
  contents: read

jobs:
  greet:
    uses: kubestellar/infra/.github/workflows/reusable-greetings.yml@main
    secrets: inherit
