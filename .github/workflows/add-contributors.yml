name: Add new contributors to readme.md

on:
  workflow_dispatch:

  push:
    branches:
      - main

jobs:
  contrib-readme-job:
    runs-on: ubuntu-latest
    name: A job to automate contributors in README
    permissions:
      contents: write
      pull-requests: write
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Run Contributors List Action
        uses: akhilmhdh/contributors-readme-action@v2.3.10
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Wait for a few seconds
        run: sleep 5
        
      - name: Fetch All Remote Branches
        run: |
          git fetch --all
          echo "Fetched all remote branches"

      - name: Detect the Branch Created by the Action
        id: get-branch
        run: |
          # Debug: Show all branches to understand the issue
          echo "Available branches:"
          git branch --all --sort=-committerdate --format='%(refname:short)'

          # Find the branch created by the contributors-readme-action
          CURRENT_BRANCH=$(git branch --all --sort=-committerdate --format='%(refname:short)' | grep "contributors-readme-action" | head -n 1)

          # Debug: Output the detected branch
          echo "CURRENT_BRANCH=$CURRENT_BRANCH" >> $GITHUB_ENV
          echo "Branch created by action: $CURRENT_BRANCH"

      - name: Configure Git
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "41898282+github-actions[bot]@users.noreply.github.com"

      - name: Amend Commit with Signed-off-by
        run: |
          git checkout -B contributors-readme-action ${{ env.CURRENT_BRANCH }}
          git commit --amend --no-edit --signoff || echo "No changes to amend"

      - name: Push Changes Back to Branch
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          git push --force-with-lease origin contributors-readme-action

      # - name: Create a Pull Request
      #   uses: peter-evans/create-pull-request@v5
      #   with:
      #     token: ${{ secrets.GITHUB_TOKEN }}
      #     base: main
      #     branch: contributors-readme-action
      #     title: "Update Contributors in README.md"
      #     body: |
      #       This PR updates the contributors list in the README.md file.
      #       It was automatically created and includes a Signed-off-by line for compliance with DCO.
