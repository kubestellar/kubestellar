name: Feedback Wanted

on:
  pull_request:
    types: [closed]

permissions:
  contents: read

jobs:
  feedback:
    uses: kubestellar/infra/.github/workflows/reusable-feedback.yml@main
    secrets: inherit
