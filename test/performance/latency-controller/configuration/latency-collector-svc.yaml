apiVersion: v1
kind: Service
metadata:
  name: latency-controller-service
  namespace: %WDS%-system
  labels:
    app: latency-controller
spec:
  selector:
    app: latency-controller
  ports:
  - name: metrics
    port: 2222
    targetPort: 2222
    protocol: TCP